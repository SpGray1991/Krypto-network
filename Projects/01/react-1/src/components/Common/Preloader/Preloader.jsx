import preloader from "../../../Img/loader.svg";

let Preloader = (props) => {
  //////////////////////////////////////////////
  return (
    <div>
      <img src={preloader} />
    </div>
  );
};

console.log("test test");

export default Preloader;
